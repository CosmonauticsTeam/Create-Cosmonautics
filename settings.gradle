pluginManagement {
    repositories {
        maven {
            url 'https://maven.neoforged.net/releases'
        }
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver' version '0.8.0'
}

rootProject.name = 'rocketnautics'

dependencyResolutionManagement {
    repositories {
        maven {
            url uri('local_maven')
        }
        mavenCentral()
        maven {
            url 'https://maven.neoforged.net/releases'
        }
    }
}

